- @title = "Random Headlines"

%table.table.table-striped
  - Headline.random.each do |headline_string|
    %tr
      %td= link_to "+ upvote", save_headlines_path(headline: headline_string), remote: true, class: "upvote-new"
      %td= headline_string
