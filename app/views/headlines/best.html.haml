%h1 Top-Voted Headlines

%ul
  - Headline.top.each do |headline|
    %li
      %strong= headline.votes
      = headline.name
      - if can_vote?(headline)
        = link_to "+ upvote", vote_headline_url(headline)