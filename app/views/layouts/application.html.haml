!!!
%html
  %head
    %title Headline Smasher | #{@title}
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
    = favicon_link_tag 'favicon.ico'
    %meta{name: 'viewport', content: 'width=device-width, initial-scale=1.0'}
    %meta{charset: 'utf-8'}
  %body
    %header
      .container
        #header-content
          = render 'layouts/login_box'

          %h1.site-title
            Headline
            %i.fa.fa-gavel
            Smasher
          .lead Fake headlines created by mashing up real headlines.

          .btn-toolbar
            .btn-group
              = link_to best_url, class: main_button_best_toggle(:order, :top, true) do
                %i.fa.fa-star
                Best Headlines
              = link_to params.merge({action: :best, controller: '/headlines', page: 1, order: :trending}), class: main_button_best_toggle(:order, :trending) do
                %i.fa.fa-flash
                Hot
              = link_to params.merge({action: :best, controller: '/headlines', page: 1, order: :new}), class: main_button_best_toggle(:order, :new) do
                %i.fa.fa-clock-o
                Recent
              / = link_to game_url, class: "btn btn-lg #{@short_title == :game ? "btn-primary" : "btn-default"}" do
              /   %i.fa.fa-trophy
              /   Headline Fight!
            - unless GENERATOR_DISABLED
              .btn-group.pull-right
                = link_to generator_url, class: "btn btn-lg btn-default #{@short_title == :generator ? "active" : "btn-default"}" do
                  %i.fa.fa-gears
                  Smash New Headlines
              .btn-group.pull-right
                = link_to leaderboard_url, class: "btn btn-lg btn-default #{@short_title == :leaderboard ? "active" : "btn-default"}" do
                  %i.fa.fa-list-ol
    .container

      - if ENV['SITE_ALERT'].present?
        #site-alert.alert.alert-warning.text-center
          %strong Hey!
          = ENV['SITE_ALERT']


      %br/

      = yield

      %hr/

      %small.text-muted
        .pull-right.text-right
          %p
            #{link_to "report a bug", "https://github.com/wilg/headlines/issues"} or #{link_to "suggest a feature", "https://github.com/wilg/headlines/issues"}
            %br/
            this app's #{link_to "source code on GitHub", "https://github.com/wilg/headlines"}
        .pull-left
          %p
            made really quickly by #{link_to "Wil Gieseler", "http://github.com/wilg"} (#{link_to "@wilgieseler", "http://twitter.com/wilgieseler"})
            %br/
            inspired by #{link_to "this awesome thing", "http://grantslatton.com/hngen/?lookback=2"} that I didn't make

    #upvoted-button{style: "display:none"}
      %button.btn.disabled{type: :button}
        %i.fa.fa-thumbs-up
        Voted
    = render 'layouts/login_apology'

  :javascript
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', '#{ ENV['GOOGLE_ANALYTICS_ID'] }', '#{ ENV['GOOGLE_ANALYTICS_URL'] }');
    ga('send', 'pageview');
